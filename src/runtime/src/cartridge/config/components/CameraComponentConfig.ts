import { ComponentConfig } from "./ComponentConfig";

import type { CameraComponent } from '../../../world/components/CameraComponent';

/**
 * Configuration data for a {@link CameraComponent}.
 * i.e. loaded from the raw cartridge file but not yet loaded into the game.
 */
export class CameraComponentConfig extends ComponentConfig {
}