import type { CameraComponent } from '@polyzone/runtime/src/world/components'

import { ComponentData } from "./ComponentData";

/**
 * Configuration data for a {@link CameraComponent}.
 * i.e. loaded from the raw cartridge file but not yet loaded into the game.
 */
export class CameraComponentData extends ComponentData {
}