import { FunctionComponent } from "react";

export const SceneList: FunctionComponent = ({ }) => {
  return (
    <div className="relative px-2 h-full overflow-y-scroll grow z-1 bg-white">
      <h3 className="text-xl">I am the scenes tab</h3>
    </div>
  );
};
